[project]
name = "composey"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "requests>=2.34.2",
    "ruff>=0.15.22",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
